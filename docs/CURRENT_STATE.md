# Текущее состояние Elysium (ответы на вопросы)

## 1. Где connection management code?

**Ответ:** В двух местах:

- **`core/src/p2p/peer.rs`** - PeerManager, ConnectionState, PeerMetrics
- **`core/src/node.rs`** - handle_connection(), run_connector(), handle_incoming_connection()

**Основные функции:**
- `handle_connection()` - обработка установленного соединения
- `run_connector()` - установка исходящих соединений
- `handle_incoming_connection()` - обработка входящих соединений
- `connect_to_peer()` - подключение к конкретному пиру

**Состояния соединений:**
- Disconnected
- Connecting
- Handshaking
- Connected
- Closing

---

## 2. AI-routing умеет forward Content messages?

**Ответ:** ✅ **ДА, умеет!**

**Где:** `core/src/node.rs` строки 1332-1407

**Как работает:**
1. Получает `ContentRequest` от пира
2. Проверяет: если контент для нас (`ely://our_id/...`) → отвечаем
3. Если не для нас и TTL > 0 → forward всем остальным пирам
4. Использует path tracking для предотвращения loops

**Код:**
```rust
Message::ContentRequest { ... } => {
    if url.starts_with(&format!("ely://{}/", self.id)) {
        // Мы владелец контента - отвечаем
    } else if ttl > 0 {
        // Forward request к другим пирам
        let forward_request = Message::ContentRequest {
            ttl: ttl - 1,
            path: forward_path,
            ...
        };
        // Отправляем всем подключенным пирам кроме отправителя
    }
}
```

**AI-routing:** Используется для выбора пиров при forward, но сейчас используется простой broadcast (все пиры кроме отправителя).

---

## 3. Есть ли TCP API для CLI?

**Ответ:** ✅ **ДА, есть!**

**Где:** `core/src/api.rs`

**Как работает:**
- TCP сервер на порту `9000 + P2P_PORT` (по умолчанию 17080 для порта 8080)
- JSON-RPC протокол (одна строка JSON на запрос)
- Команды: send, broadcast, peers, status, ping, inbox, watch, publish, fetch, name_register, name_resolve, bundle_export, bundle_import, bundle_info

**Пример запроса:**
```json
{"command": "status"}
```

**Пример ответа:**
```json
{"success": true, "data": {"node_id": "...", "connected_peers": 2, ...}}
```

**CLI использует:** Прямо TCP API через `TcpStream` в `cli_app.rs`

---

## 4. Сколько времени реально можешь тратить?

**Это вопрос к тебе!** 😊

Но судя по плану:
- Week 1-4: 60-80 часов (15-20 часов/неделю)
- Это ~2-3 часа в день

---

## ✅ Что уже работает

### Connection Management
- ✅ TCP connections (incoming/outgoing)
- ✅ Handshake с RSA encryption
- ✅ AES-GCM session encryption
- ✅ Heartbeat/keepalive
- ✅ Auto-reconnect (через connector)
- ✅ Connection state management

### Content Protocol
- ✅ ContentRequest/ContentResponse messages
- ✅ Forwarding через mesh (TTL-based)
- ✅ Path tracking (loop prevention)
- ✅ Local content store (sled)

### API & CLI
- ✅ TCP API server
- ✅ CLI через TCP API
- ✅ Все команды работают

### Web Gateway
- ✅ HTTP server для просмотра ely:// контента
- ✅ URL handler (OS-level)
- ✅ Chrome extension

---

## ❌ Что нужно добавить (по твоему плану)

### Week 1: Heartbeat + Reconnection
- ⚠️ Heartbeat есть, но может нужны улучшения
- ⚠️ Reconnection есть (connector), но может нужны улучшения

### Week 2: Content Store + Protocol
- ✅ Content Store есть (sled)
- ✅ Protocol есть (ContentRequest/Response)
- ⚠️ Может нужны улучшения в routing

### Week 3: CLI + Request/Response
- ✅ CLI есть
- ✅ Request/Response есть
- ⚠️ Может нужны улучшения в UX

### Week 4: HTTP Gateway
- ✅ HTTP Gateway есть (Web Gateway)
- ⚠️ Может нужны улучшения (например, отдельный порт)

---

## 🧪 Тесты из твоего плана

### Test 1: `ely start 8080 --gateway 8000`
**Статус:** ❌ Нет флага `--gateway`

**Нужно добавить:**
- Парсинг `--gateway <port>` в Config
- Запуск Web Gateway на указанном порту

### Test 2: `ely publish ./test_site`
**Статус:** ❌ Нет поддержки публикации директорий

**Сейчас:** `ely publish <path> <content>` - только один файл

**Нужно добавить:**
- Рекурсивная публикация директории
- Поддержка `ely publish <dir>` для публикации всех файлов

### Test 3: `http://localhost:8000/ely/<node_id>/test_site/index.html`
**Статус:** ⚠️ Частично работает

**Сейчас:** `http://localhost:17081/e/<base64>` формат

**Нужно добавить:**
- Поддержка `/ely/<node_id>/<path>` формата
- Или изменить формат на более читаемый

### Test 4: Multi-node fetch через mesh
**Статус:** ✅ Должно работать

**Проверка:** Нужно протестировать на 3 узлах

---

## 📋 Конкретный TODO для Week 1-4

### Week 1: Улучшения Connection Management

1. **Добавить флаг `--gateway <port>`**
   - Файл: `core/src/config.rs`
   - Добавить `gateway_port: Option<u16>`
   - Парсинг в `from_args()`

2. **Улучшить reconnection logic**
   - Файл: `core/src/node.rs` (run_connector)
   - Добавить exponential backoff
   - Улучшить обработку ошибок

### Week 2: Content Store улучшения

1. **Рекурсивная публикация директорий**
   - Файл: `core/src/cli_app.rs` (publish command)
   - Добавить `ely publish <dir>` для публикации всех файлов

2. **Улучшить Content routing**
   - Файл: `core/src/node.rs` (ContentRequest handling)
   - Использовать AI-routing для выбора пиров при forward

### Week 3: CLI улучшения

1. **Улучшить UX команд**
   - Более понятные сообщения об ошибках
   - Прогресс-бары для длительных операций

### Week 4: HTTP Gateway улучшения

1. **Поддержка `/ely/<node_id>/<path>` формата**
   - Файл: `core/src/web_gateway.rs`
   - Добавить route для `/ely/<node_id>/<path>`
   - Fallback на `/e/<base64>` для совместимости

2. **Отдельный порт для gateway**
   - Уже работает через `--gateway` (нужно добавить)

---

## 🚀 Быстрый старт для тестирования

```bash
# Terminal 1: Node A
ely start 8080

# Terminal 2: Node B  
ely start 8081 127.0.0.1:8080

# Terminal 3: Node C
ely start 8082 127.0.0.1:8081

# Terminal 4: Publish from A
ely publish site/test.html "<h1>Hello</h1>"
# Скопируй node_id из вывода

# Terminal 5: Fetch from C (через B)
ely fetch ely://<A_node_id>/site/test.html
# Должно вернуть: <h1>Hello</h1>
```

---

## 💡 Рекомендации

1. **Начни с Week 1** - добавь `--gateway` флаг
2. **Протестируй multi-node fetch** - убедись что forwarding работает
3. **Добавь рекурсивную публикацию** - для удобства
4. **Улучши URL формат** - `/ely/<node_id>/<path>` вместо `/e/<base64>`

Готов помочь с реализацией любого из этих пунктов!
