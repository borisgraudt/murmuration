# 🎬 План для демонстрационного видео

**Цель:** Создать 2-3 минутное видео, демонстрирующее Elysium Protocol  
**Аудитория:** Техническая конференция (разработчики, исследователи)  
**Формат:** Скринкаст с голосовым комментарием

---

## 📋 Подготовка перед записью

### 1. Подготовить тестовые данные

**Создать тестовый сайт:**
```bash
mkdir -p demo_site
cat > demo_site/index.html << 'EOF'
<!DOCTYPE html>
<html>
<head>
    <title>Elysium Demo</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>🌐 Welcome to Elysium</h1>
    <p>This site is hosted on a decentralized mesh network.</p>
    <p>No servers. No internet required.</p>
    <img src="logo.png" alt="Elysium Logo">
</body>
</html>
EOF

cat > demo_site/style.css << 'EOF'
body {
    font-family: system-ui, sans-serif;
    max-width: 800px;
    margin: 50px auto;
    padding: 20px;
    background: #f0f0f0;
}
h1 { color: #2563eb; }
EOF

# Создать простой логотип (или использовать существующий)
# Можно использовать любой PNG изображение
```

### 2. Подготовить терминалы

**Открыть 3-4 терминала:**
- Terminal 1: Первый узел (logs)
- Terminal 2: Второй узел (logs)
- Terminal 3: CLI команды
- Terminal 4: Браузер (опционально)

### 3. Проверить, что все работает

```bash
# Запустить узлы
ely start 8080 --gateway 8000 -d
ely start 8081 127.0.0.1:8080 --gateway 8001 -d

# Проверить статус
ely status --port 17080
ely status --port 17081

# Убедиться, что узлы подключены
ely peers --port 17080
```

---

## 🎥 Сценарий видео (2-3 минуты)

### Вступление (10-15 секунд)

**Что показать:**
- Заголовок: "Elysium Protocol - Decentralized Mesh Network"
- Краткое описание: "Интернет без интернета. Полностью децентрализованная сеть."

**Что сказать:**
> "Привет! Сегодня я покажу Elysium Protocol - децентрализованную mesh сеть, которая работает без серверов и даже без интернета."

---

### Часть 1: Запуск сети (30 секунд)

**Что показать:**
```bash
# Terminal 1
ely start 8080 --gateway 8000

# Terminal 2 (после запуска первого)
ely start 8081 127.0.0.1:8080 --gateway 8001

# Показать логи подключения
# Показать статус узлов
ely status --port 17080
ely status --port 17081
```

**Что сказать:**
> "Запускаю два узла. Они автоматически находят друг друга через UDP discovery и устанавливают P2P соединение. Видите, как они подключились? Каждый узел имеет уникальный Node ID."

**Визуально:**
- Показать логи подключения
- Подчеркнуть "Connected: 1/1 peers"
- Показать разные Node ID

---

### Часть 2: Публикация контента (30 секунд)

**Что показать:**
```bash
# Показать структуру demo_site
ls -la demo_site/

# Опубликовать рекурсивно
ely publish site @demo_site --port 17080

# Показать вывод с Node ID и URL
```

**Что сказать:**
> "Теперь опубликую сайт на первом узле. Использую рекурсивную публикацию - все файлы из директории автоматически публикуются. Видите, получился URL: `ely://<node_id>/site/index.html`."

**Визуально:**
- Показать структуру директории
- Показать процесс публикации
- Подчеркнуть полученный `ely://` URL

---

### Часть 3: Получение контента через mesh (30 секунд)

**Что показать:**
```bash
# Получить контент со второго узла (через forwarding)
ely fetch ely://<node_id_from_node1>/site/index.html --port 17081

# Показать полученный HTML
```

**Что сказать:**
> "Теперь получу контент со второго узла. Контент автоматически пересылается через mesh сеть. Видите, HTML получен успешно, хотя он был опубликован на первом узле."

**Визуально:**
- Показать команду fetch
- Показать полученный контент
- Подчеркнуть, что это работает через forwarding

---

### Часть 4: Web Gateway в браузере (30 секунд)

**Что показать:**
```bash
# Открыть браузер
# Перейти на http://localhost:8000/ely/<node_id>/site/index.html
```

**Что сказать:**
> "Теперь открою этот контент в обычном браузере. Web Gateway автоматически преобразует `ely://` URL в HTTP. Видите, сайт открылся! Можно использовать чистый формат URL: `/ely/<node_id>/<path>`."

**Визуально:**
- Показать браузер
- Показать URL в адресной строке
- Показать отрендеренный сайт
- Упомянуть Chrome extension (если установлен)

---

### Часть 5: Bundle Protocol (20 секунд)

**Что показать:**
```bash
# Export bundle
ely bundle export demo.bundle --port 17080

# Показать информацию о bundle
ely bundle info demo.bundle

# Импортировать на другом узле (опционально)
# ely bundle import demo.bundle --port 17081
```

**Что сказать:**
> "И наконец, Bundle Protocol - можно экспортировать все сообщения в файл и передать физически (например, через USB). Это позволяет работать полностью офлайн."

**Визуально:**
- Показать создание bundle
- Показать информацию о bundle
- Подчеркнуть offline capability

---

### Заключение (10 секунд)

**Что показать:**
- Финальный слайд с ключевыми фичами:
  - ✅ Децентрализовано (нет серверов)
  - ✅ Работает офлайн
  - ✅ End-to-end шифрование
  - ✅ Bundle protocol для физической передачи

**Что сказать:**
> "Elysium Protocol - полностью децентрализованная mesh сеть, которая работает без интернета. Спасибо за внимание!"

---

## 🎬 Технические детали записи

### Инструменты для записи

**macOS:**
- QuickTime Player (встроенный) - `⌘ + Shift + 5`
- OBS Studio (бесплатный, более продвинутый)
- ScreenFlow (платный, профессиональный)

**Linux:**
- OBS Studio
- SimpleScreenRecorder
- `ffmpeg` (командная строка)

**Windows:**
- OBS Studio
- Windows Game Bar (`Win + G`)

### Настройки записи

**Рекомендуемые настройки:**
- Разрешение: 1920x1080 (Full HD)
- FPS: 30 (достаточно для скринкаста)
- Формат: MP4 (H.264)
- Качество: High

**Аудио:**
- Использовать хороший микрофон
- Проверить уровень звука перед записью
- Убрать фоновый шум

### Подготовка экрана

**Перед записью:**
1. Закрыть лишние приложения
2. Убрать уведомления
3. Увеличить размер шрифта в терминале (для читаемости)
4. Использовать темную тему (меньше устают глаза)
5. Подготовить все терминалы заранее

**Размещение окон:**
```
┌─────────────┬─────────────┐
│ Terminal 1  │ Terminal 2  │
│ (Node 1)    │ (Node 2)    │
├─────────────┼─────────────┤
│ Terminal 3  │ Browser     │
│ (CLI)       │ (Gateway)   │
└─────────────┴─────────────┘
```

---

## 📝 Чеклист перед записью

- [ ] Тестовый сайт создан и протестирован
- [ ] Все узлы запускаются без ошибок
- [ ] CLI команды работают корректно
- [ ] Web Gateway открывается в браузере
- [ ] Bundle export/import работает
- [ ] Микрофон проверен, звук записывается
- [ ] Экран подготовлен (закрыты лишние окна)
- [ ] Терминалы настроены (большой шрифт, читаемые цвета)
- [ ] Скрипт/план готов (можно читать во время записи)

---

## 🎯 Ключевые моменты для демонстрации

1. **Автоматическое подключение** - узлы находят друг друга автоматически
2. **Рекурсивная публикация** - `ely publish site @dir` работает просто
3. **Чистые URL** - `/ely/<node_id>/<path>` вместо base64
4. **Web Gateway** - работает в обычном браузере
5. **Offline capability** - Bundle protocol для физической передачи

---

## 💡 Советы

1. **Репетируй перед записью** - прогони весь сценарий 2-3 раза
2. **Говори четко** - не торопись, объясняй что делаешь
3. **Показывай результат** - не просто команды, но и их вывод
4. **Используй паузы** - дай зрителю время прочитать
5. **Делай несколько дублей** - можно потом выбрать лучший

---

## 📦 После записи

1. **Монтаж** (опционально):
   - Обрезать начало/конец
   - Добавить заголовки/титры
   - Улучшить звук (если нужно)

2. **Экспорт:**
   - Формат: MP4 (H.264)
   - Разрешение: 1920x1080
   - Размер: стараться < 100MB (для загрузки)

3. **Публикация:**
   - YouTube (unlisted для конференции)
   - Vimeo
   - Или просто файл для показа

---

## 🚀 Быстрый старт

```bash
# 1. Подготовить тестовый сайт
mkdir -p demo_site
echo "<h1>Elysium Demo</h1>" > demo_site/index.html

# 2. Запустить узлы
ely start 8080 --gateway 8000 -d
ely start 8081 127.0.0.1:8080 --gateway 8001 -d

# 3. Подождать 2-3 секунды для подключения

# 4. Опубликовать
ely publish site @demo_site --port 17080

# 5. Получить Node ID из вывода и использовать в дальнейшем

# 6. Записать видео по сценарию выше
```

**Готово! Удачи с записью! 🎬**
