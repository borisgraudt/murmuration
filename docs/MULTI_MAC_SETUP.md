# 🖥️ Запуск Elysium на двух Mac'ах

Пошаговая инструкция для запуска двух узлов на разных Mac'ах в одной сети.

---

## 📋 Требования

- Два Mac'а в одной Wi‑Fi сети (или подключены к одному роутеру)
- Rust установлен на обоих Mac'ах
- Elysium установлен на обоих Mac'ах (`make install`)

---

## 🚀 Шаг 1: Узнать IP адреса

### На Mac #1:
```bash
# Узнать локальный IP адрес
ifconfig | grep "inet " | grep -v 127.0.0.1
# Или проще:
ipconfig getifaddr en0
# Или для Wi-Fi:
ipconfig getifaddr en1
```

**Пример:** `192.168.1.100`

### На Mac #2:
```bash
ipconfig getifaddr en0
```

**Пример:** `192.168.1.101`

---

## 🚀 Шаг 2: Запустить узел на Mac #1

**На Mac #1 (IP: 192.168.1.100):**

```bash
# Запустить узел на порту 8080
ely start 8080

# Или с кастомным gateway портом:
ely start 8080 --gateway 8000
```

**Вывод должен показать:**
```
Node ID: <node_id_1>
Listening on 0.0.0.0:8080
API server listening on 127.0.0.1:17080
Web Gateway started on http://127.0.0.1:17081
Discovery: enabled (port 9998)
```

**⚠️ Важно:** Запиши `Node ID` из вывода - он понадобится для Mac #2!

---

## 🚀 Шаг 3: Запустить узел на Mac #2

**На Mac #2 (IP: 192.168.1.101):**

```bash
# Подключиться к Mac #1, указав его IP адрес
ely start 8081 192.168.1.100:8080

# Или с кастомным gateway:
ely start 8081 192.168.1.100:8080 --gateway 8001
```

**Вывод должен показать:**
```
Node ID: <node_id_2>
Connecting to 192.168.1.100:8080...
✓ Connected to peer
API server listening on 127.0.0.1:17081
Web Gateway started on http://127.0.0.1:17082
```

---

## ✅ Шаг 4: Проверить подключение

### На Mac #1:
```bash
# Проверить список пиров
ely peers
```

**Должно показать:**
```
Connected peers:
  <node_id_2> - 192.168.1.101:8081 (Connected)
```

### На Mac #2:
```bash
ely peers
```

**Должно показать:**
```
Connected peers:
  <node_id_1> - 192.168.1.100:8080 (Connected)
```

---

## 🧪 Шаг 5: Протестировать обмен сообщениями

### На Mac #1:
```bash
# Отправить сообщение на Mac #2
ely send <node_id_2> "Hello from Mac #1!"

# Или broadcast всем:
ely broadcast "Hello everyone!"
```

### На Mac #2:
```bash
# Посмотреть входящие сообщения
ely inbox

# Или смотреть в реальном времени:
ely watch
```

---

## 🌐 Шаг 6: Протестировать публикацию контента

### На Mac #1:
```bash
# Публиковать контент
ely publish site/index.html "<h1>Hello from Mac #1</h1>"

# Или рекурсивно публиковать директорию:
ely publish site @./my_site
```

**Скопируй `Node ID` из вывода!**

### На Mac #2:
```bash
# Получить контент с Mac #1
ely fetch ely://<node_id_1>/site/index.html

# Открыть в браузере через Web Gateway
open http://localhost:17082/ely/<node_id_1>/site/index.html
```

---

## 🔧 Troubleshooting

### Проблема: Узлы не подключаются

**Решение 1: Проверь firewall**
```bash
# На macOS может блокировать входящие соединения
# Разреши в System Settings > Network > Firewall
```

**Решение 2: Проверь, что порты открыты**
```bash
# На Mac #1 проверь, что порт 8080 слушается
lsof -i :8080

# На Mac #2 проверь, что порт 8081 слушается
lsof -i :8081
```

**Решение 3: Используй явный IP адрес**
```bash
# Вместо localhost используй реальный IP
ely start 8081 192.168.1.100:8080
```

### Проблема: Discovery не работает

**Решение:** Discovery работает только в одной подсети. Если Mac'и в разных сетях, используй явное подключение:
```bash
ely start 8081 <IP_MAC_1>:8080
```

### Проблема: "Address already in use"

**Решение:** Останови старые узлы:
```bash
# Найти и убить процессы
killall ely core

# Или найти конкретный процесс
lsof -ti:8080 | xargs kill
```

---

## 📝 Быстрая команда для копирования

### Mac #1:
```bash
# Запустить узел
ely start 8080 --gateway 8000

# В другом терминале - проверить статус
ely status
ely peers
```

### Mac #2:
```bash
# Подключиться к Mac #1 (замени IP на реальный!)
ely start 8081 <IP_MAC_1>:8080 --gateway 8001

# В другом терминале - проверить статус
ely status
ely peers
```

---

## 🎯 Пример полного workflow

### Mac #1 (Alice):
```bash
# 1. Запустить узел
ely start 8080 --gateway 8000

# 2. Публиковать сайт
ely publish site @./my_website

# 3. Скопировать Node ID из вывода
# Node ID: 7Sv67Ht7JBoNDyNotL6Ms47gHoDPJHi1P97SKvLxw5SM
```

### Mac #2 (Bob):
```bash
# 1. Подключиться к Alice
ely start 8081 192.168.1.100:8080 --gateway 8001

# 2. Получить контент от Alice
ely fetch ely://7Sv67Ht7JBoNDyNotL6Ms47gHoDPJHi1P97SKvLxw5SM/site/index.html

# 3. Открыть в браузере
open http://localhost:8001/ely/7Sv67Ht7JBoNDyNotL6Ms47gHoDPJHi1P97SKvLxw5SM/site/index.html
```

---

## 💡 Советы

1. **Используй разные порты** на каждом Mac'е (8080, 8081, 8082...)
2. **Используй `--gateway`** для удобных портов (8000, 8001, 8002...)
3. **Discovery работает автоматически** - узлы найдут друг друга в одной сети
4. **Для разных сетей** используй явное подключение через IP
5. **Проверь firewall** если узлы не подключаются

---

Готово! Теперь у тебя два узла работают в одной сети! 🎉
