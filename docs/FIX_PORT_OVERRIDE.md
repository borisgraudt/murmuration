# 🔧 Исправление: --port флаг игнорируется

## Проблема

Когда используется `--port` флаг, команда все равно подключается к порту из файла `~/.elysium_api_port`.

**Причина:** Файл `~/.elysium_api_port` перезаписывается последним запущенным узлом, и если он доступен, код использует его вместо указанного `--port`.

## Временное решение

**Удалить файл `~/.elysium_api_port`:**
```bash
rm ~/.elysium_api_port
```

После этого `--port` флаг будет работать правильно.

## Постоянное решение

Код уже исправлен - `--port` флаг имеет наивысший приоритет. Но файл `~/.elysium_api_port` все еще может мешать, если он существует.

**Рекомендация:** Удалить файл перед использованием нескольких узлов:
```bash
# Перед запуском нескольких узлов
rm ~/.elysium_api_port

# Затем запустить узлы
ely start 8080 --gateway 8000 -d
ely start 8081 127.0.0.1:8080 --gateway 8001 -d
ely start 8082 127.0.0.1:8081 --gateway 8002 -d

# Теперь --port будет работать правильно
ely status --port 17080  # Узел 1
ely status --port 17082  # Узел 2
ely status --port 17084  # Узел 3
```

## Проверка

После удаления файла проверьте:
```bash
# Должны показать разные Node ID
ely status --port 17080
ely status --port 17082
ely status --port 17084
```

Каждый должен показать уникальный Node ID!
