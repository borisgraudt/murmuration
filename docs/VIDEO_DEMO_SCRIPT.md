# 🎬 Демонстрационный скрипт для видео

**Важно:** Все команды должны использовать `--port` флаг для указания правильного API порта!

---

## 📋 Подготовка

### 1. Остановить все запущенные узлы

```bash
# Найти и остановить все процессы ely
pkill -f "ely start"

# Или по PID (из вывода ely start -d)
kill 78233 78341 78431
```

### 2. Очистить старые данные (опционально)

```bash
# Удалить старые директории (будет созданы новые с правильными Node ID)
rm -rf .ely/node-8080 .ely/node-8081 .ely/node-8082
```

---

## 🎥 Сценарий записи

### Шаг 1: Запустить узлы (30 секунд)

**Terminal 1:**
```bash
ely start 8080 --gateway 8000 -d
# Запомнить: P2P 8080 → API 17080
```

**Terminal 2:**
```bash
ely start 8081 127.0.0.1:8080 --gateway 8001 -d
# Запомнить: P2P 8081 → API 17082 (9000 + 8081 = 17081, но может быть занят, поэтому 17082)
```

**Terminal 3:**
```bash
ely start 8082 127.0.0.1:8081 --gateway 8002 -d
# Запомнить: P2P 8082 → API 17083 (9000 + 8082 = 17082, но может быть занят, поэтому 17083)
```

**Подождать 2-3 секунды для подключения**

---

### Шаг 2: Проверить статус каждого узла (30 секунд)

**Terminal 4 (для команд):**

```bash
# Проверить первый узел (P2P 8080 → API 17080)
ely status --port 17080
# Должен показать: Node ID: <unique_id_1>, Connected: 1/3 peers

# Проверить второй узел (P2P 8081 → API 17082)
ely status --port 17082
# Должен показать: Node ID: <unique_id_2>, Connected: 2/3 peers

# Проверить третий узел (P2P 8082 → API 17083)
ely status --port 17083
# Должен показать: Node ID: <unique_id_3>, Connected: 2/3 peers
```

**Важно:** Node ID должны быть РАЗНЫМИ!

---

### Шаг 3: Показать пиры (20 секунд)

```bash
# Показать пиры первого узла
ely peers --port 17080

# Показать пиры второго узла
ely peers --port 17082

# Показать пиры третьего узла
ely peers --port 17083
```

---

### Шаг 4: Публикация контента (30 секунд)

**Создать тестовый сайт:**
```bash
mkdir -p demo_site
echo "<h1>Elysium Demo</h1>" > demo_site/index.html
echo "body { font-family: sans-serif; }" > demo_site/style.css
```

**Опубликовать на первом узле:**
```bash
ely publish site @demo_site --port 17080
# Скопировать Node ID из вывода (например: FzmzADh51zHLPNpea9pTVY3ynyjZTuN1VNWji5sf7Jk1)
```

**Запомнить URL:** `ely://<node_id_1>/site/index.html`

---

### Шаг 5: Получение контента через mesh (30 секунд)

**Получить контент на третьем узле (через второй):**
```bash
# Использовать Node ID из шага 4
ely fetch ely://<node_id_1>/site/index.html --port 17083
# Должно вернуть: <h1>Elysium Demo</h1>
```

**Показать, что это работает через forwarding:**
```bash
# Показать логи третьего узла
tail -f .ely/node-8082/node-8082.log | grep -i "forwarding\|content"
```

---

### Шаг 6: Web Gateway в браузере (30 секунд)

**Открыть в браузере:**
```
http://localhost:8000/ely/<node_id_1>/site/index.html
```

**Показать:**
- URL в адресной строке
- Отрендеренный сайт
- Упомянуть Chrome extension (если установлен)

---

### Шаг 7: Bundle Protocol (20 секунд)

```bash
# Export bundle с первого узла
ely bundle export demo.bundle --port 17080

# Показать информацию о bundle
ely bundle info demo.bundle
```

---

## ⚠️ КРИТИЧНО: Всегда используйте --port!

**Неправильно:**
```bash
ely status        # Подключится к первому найденному узлу (может быть любой)
ely status        # Подключится к тому же узлу
ely status        # Подключится к тому же узлу
```

**Правильно:**
```bash
ely status --port 17080  # Первый узел (P2P 8080)
ely status --port 17082  # Второй узел (P2P 8081)
ely status --port 17083  # Третий узел (P2P 8082)
```

---

## 📊 Таблица портов для видео

| Узел | P2P порт | API порт | Gateway порт | Команда для проверки |
|------|----------|----------|--------------|----------------------|
| 1    | 8080     | 17080    | 8000         | `ely status --port 17080` |
| 2    | 8081     | 17082    | 8001         | `ely status --port 17082` |
| 3    | 8082     | 17083    | 8002         | `ely status --port 17083` |

**Примечание:** API порт может отличаться от `9000 + P2P порт`, если порт занят. Система автоматически выбирает следующий доступный порт.

---

## 🔍 Как узнать правильный API порт?

**Способ 1: Из логов узла**
```bash
# Посмотреть логи узла
grep "API server" .ely/node-8080/node-8080.log
# Должно показать: API server listening on 127.0.0.1:17080
```

**Способ 2: Проверить открытые порты**
```bash
lsof -i -P | grep LISTEN | grep ely
# Показать все открытые порты ely
```

**Способ 3: Попробовать стандартные порты**
```bash
# API порт обычно = 9000 + P2P порт
# Но может быть занят, поэтому система выбирает следующий
ely status --port 17080  # Для P2P 8080
ely status --port 17081  # Может быть Gateway для 8080
ely status --port 17082  # Для P2P 8081
ely status --port 17083  # Для P2P 8082
```

---

## 💡 Советы для записи

1. **Всегда используйте --port** - это ключевой момент для демонстрации нескольких узлов
2. **Показывайте разные Node ID** - подчеркните, что каждый узел уникален
3. **Показывайте процесс подключения** - как узлы находят друг друга
4. **Демонстрируйте forwarding** - контент проходит через mesh сеть
5. **Говорите четко** - объясняйте, что делаете и почему

---

## 🎯 Ключевые моменты для демонстрации

1. ✅ **Каждый узел имеет уникальный Node ID**
2. ✅ **Узлы автоматически находят друг друга**
3. ✅ **Контент пересылается через mesh сеть**
4. ✅ **Web Gateway работает в обычном браузере**
5. ✅ **Bundle Protocol для offline передачи**

**Готово! Удачи с записью! 🎬**
