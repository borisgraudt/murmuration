# 🧪 Тестирование нескольких узлов

## Быстрая проверка

После исправления `--port` флага, проверьте что все работает:

```bash
# 1. Остановить все узлы
pkill -f "ely start"

# 2. Удалить файл, который мешает (если есть)
rm ~/.elysium_api_port

# 3. Запустить узлы
ely start 8080 --gateway 8000 -d
ely start 8081 127.0.0.1:8080 --gateway 8001 -d
ely start 8082 127.0.0.1:8081 --gateway 8002 -d

# 4. Подождать 2-3 секунды

# 5. Проверить каждый узел (должны быть РАЗНЫЕ Node ID!)
ely status --port 17080  # Узел 1
ely status --port 17082  # Узел 2
ely status --port 17084  # Узел 3
```

## Ожидаемый результат

Каждый `ely status --port <api_port>` должен показать:
- ✅ Уникальный Node ID (разный для каждого узла)
- ✅ Разное количество подключенных пиров
- ✅ Сообщение: "✓ Connecting to API on port <port> (specified with --port)"

## Если все еще не работает

1. **Проверьте, что узлы запущены:**
   ```bash
   ps aux | grep "ely start" | grep -v grep
   ```

2. **Проверьте, на каких портах они слушают:**
   ```bash
   lsof -i -P | grep LISTEN | grep ely
   ```

3. **Проверьте логи узлов:**
   ```bash
   tail -20 .ely/node-8080/node-8080.log | grep "API server"
   tail -20 .ely/node-8081/node-8081.log | grep "API server"
   tail -20 .ely/node-8082/node-8082.log | grep "API server"
   ```

4. **Убедитесь, что используете правильные API порты:**
   - P2P 8080 → API обычно 17080 (но может быть другой, если занят)
   - P2P 8081 → API обычно 17082 (но может быть другой, если занят)
   - P2P 8082 → API обычно 17084 (но может быть другой, если занят)

   **Проверьте из логов!**

## Правильные команды для видео

```bash
# Узнать API порт из логов
grep "API server" .ely/node-8080/node-8080.log
# Должно показать: API server listening on 127.0.0.1:17080

# Использовать этот порт
ely status --port 17080
```

**Важно:** API порт может отличаться от `9000 + P2P порт`, если порт занят. Всегда проверяйте из логов!
