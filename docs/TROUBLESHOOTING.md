# Troubleshooting Guide

## Проблема: Второй узел не подключается

### Проверка 1: Убедитесь, что первый узел запущен

```bash
# Проверьте, запущен ли узел на порту 8080
lsof -ti:8080
```

Если команда ничего не выводит, узел не запущен. Запустите его:

```bash
cd core
MESHLINK_API_PORT=17080 cargo run --bin core --release -- 8080
```

### Проверка 2: Убедитесь, что порты свободны

```bash
# Проверьте порты 8080 и 8081
lsof -ti:8080,8081
```

Если порты заняты, либо остановите процессы, либо используйте другие порты.

### Проверка 3: Правильный формат адреса

При запуске второго узла используйте правильный формат:

```bash
# Правильно:
cargo run --bin core --release -- 8081 127.0.0.1:8080

# Неправильно:
cargo run --bin core --release -- 8081 localhost:8080  # может не работать
```

### Проверка 4: Проверьте логи первого узла

В терминале первого узла вы должны увидеть:
```
Accepting incoming connection from 127.0.0.1:xxxxx
Handshake successful with 127.0.0.1:xxxxx (ID: ..., protocol: 1)
```

Если видите ошибки handshake, проверьте:
- Версии протокола совместимы
- Ключи шифрования правильно сгенерированы

### Проверка 5: Используйте Python CLI для проверки

```bash
# Проверьте статус первого узла
MESHLINK_API_PORT=17080 python3 python_cli/cli.py peers

# Проверьте статус второго узла
MESHLINK_API_PORT=17081 python3 python_cli/cli.py peers
```

Оба должны показывать друг друга в списке пиров.

## Проблема: Python CLI не показывает цвета

### Решение 1: Убедитесь, что используете правильный терминал

Rich требует терминал с поддержкой цветов. Используйте:
- Terminal.app (macOS)
- iTerm2 (macOS)
- Windows Terminal (Windows)
- xterm/gnome-terminal (Linux)

### Решение 2: Проверьте переменную окружения

```bash
# Убедитесь, что цвета включены
export TERM=xterm-256color
python3 python_cli/cli.py status
```

### Решение 3: Принудительно включите цвета

```bash
FORCE_COLOR=1 python3 python_cli/cli.py status
```

## Проблема: Веб-интерфейс не показывает узлы

### Проверка 1: Убедитесь, что узлы запущены

Веб-интерфейс подключается к API на порту 8000 (FastAPI backend). Убедитесь, что:
1. Rust узлы запущены
2. FastAPI backend запущен (если используется)

### Проверка 2: Проверьте консоль браузера

Откройте Developer Tools (F12) и проверьте ошибки в консоли.

### Проверка 3: Проверьте API endpoint

```bash
# Проверьте, отвечает ли API
curl http://localhost:8000/api/status
```

## Проблема: Сообщения не отправляются

### Проверка 1: Убедитесь, что пиры подключены

```bash
MESHLINK_API_PORT=17080 python3 python_cli/cli.py peers
```

Должен показать хотя бы одного пира со статусом `Connected`.

### Проверка 2: Используйте правильный peer_id

```bash
# Получите ID пира
MESHLINK_API_PORT=17080 python3 python_cli/cli.py peers

# Используйте полный ID (не сокращенный)
MESHLINK_API_PORT=17080 python3 python_cli/cli.py send "полный-peer-id" "Сообщение"
```

### Проверка 3: Проверьте логи узлов

В логах получателя должно появиться:
```
📨 Received data message ... from ...: ... bytes
   Content: "ваше сообщение"
```

## Общие советы

1. **Всегда запускайте узлы из директории `core/`**
2. **Используйте разные терминалы для каждого узла**
3. **Проверяйте логи узлов на наличие ошибок**
4. **Убедитесь, что порты не заняты другими процессами**
5. **Используйте `127.0.0.1` вместо `localhost` для надежности**


