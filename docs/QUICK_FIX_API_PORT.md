# 🔧 Быстрое решение: Как найти правильный API порт

## Проблема

Если вы видите одинаковый Node ID при использовании разных `--port`, значит вы подключаетесь к одному и тому же узлу.

## Решение

**API порт = 9000 + P2P порт**

Если узел запущен на P2P порту `8083`, то API порт = `17083` (9000 + 8083).

### Как узнать P2P порт узла?

**Способ 1: Из логов**
```bash
# Посмотреть логи узла
cat .ely/node-8083/node-8083.log | grep "Listening on"
# Должно показать: Listening on: 0.0.0.0:8083
```

**Способ 2: Из имени директории**
```bash
# Если узел запущен в daemon mode, логи в:
.ely/node-<P2P_PORT>/node-<P2P_PORT>.log

# Например:
.ely/node-8083/node-8083.log  # P2P порт = 8083, API порт = 17083
.ely/node-8080/node-8080.log  # P2P порт = 8080, API порт = 17080
.ely/node-8081/node-8081.log  # P2P порт = 8081, API порт = 17081
```

**Способ 3: Попробовать разные порты**
```bash
# Попробовать стандартные порты
ely status --port 17080  # Для узла на P2P 8080
ely status --port 17081  # Для узла на P2P 8081
ely status --port 17082  # Для узла на P2P 8082
ely status --port 17083  # Для узла на P2P 8083
```

---

## Пример

Если вы запустили:
```bash
ely start 8083 --gateway 8003 -d
```

То:
- **P2P порт:** 8083
- **API порт:** 17083 (9000 + 8083)
- **Gateway порт:** 8003 (указан явно)

Используйте:
```bash
ely status --port 17083
ely publish site/index.html "<h1>Test</h1>" --port 17083
```

---

## Улучшение: Автоматическое определение

В будущем можно добавить команду:
```bash
ely nodes  # Показать все запущенные узлы и их API порты
```

Но пока используйте формулу: **API порт = 9000 + P2P порт**
