# Быстрый старт - Тестирование в одной Wi-Fi сети

## Шаг 1: Узнайте IP адреса устройств

### На ноутбуке (macOS):
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```
Вы увидите что-то вроде: `inet 192.168.1.100 netmask 0xffffff00`

### На телефоне (Android через ADB):
```bash
adb shell ip addr show wlan0 | grep "inet "
```

Или просто зайдите в настройки Wi-Fi на телефоне и посмотрите IP адрес.

## Шаг 2: Запустите ноды

### На ноутбуке (порт 8080):
```bash
cd /Users/boris/Desktop/meshlink
cargo run --bin core -- 8080
```

Вы увидите:
```
🚀 Starting MeshLink P2P Node
   Node ID: <uuid>
   Protocol Version: 1
Listening on: 0.0.0.0:8080
```

### На телефоне (порт 8081, указывая IP ноутбука):

**Если на телефоне установлен Rust:**
```bash
cargo run --bin core -- 8081 192.168.1.100:8080
```

**Если используете ADB:**
```bash
# На ноутбуке выполните:
adb reverse tcp:8081 tcp:8081
adb shell "cd /path/to/meshlink && cargo run --bin core -- 8081 192.168.1.100:8080"
```

## Шаг 3: Проверьте подключение

Вы должны увидеть в логах:

**На ноутбуке:**
```
Incoming connection from 192.168.1.101:xxxxx
Handshake successful with 192.168.1.101 (ID: <peer_id>, protocol: 1)
Connected: <peer_id>
Heartbeat - Connected: 1/1 peers
```

**На телефоне:**
```
TCP connection established to 192.168.1.100:8080
Connected to peer 192.168.1.100:8080 (ID: <peer_id>, protocol: 1)
Connected: <peer_id>
Heartbeat - Connected: 1/1 peers
```

## Шаг 4: Проверьте шифрование

В логах должны быть сообщения:
```
Decrypted message from <peer_id>
```

Это означает, что сообщения шифруются и расшифровываются правильно.

## Отладка

### Если соединение не устанавливается:

1. **Проверьте firewall:**
   ```bash
   # macOS
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate
   ```

2. **Проверьте, что порты открыты:**
   ```bash
   # На ноутбуке
   lsof -i :8080
   
   # Должно показать процесс cargo/rust
   ```

3. **Проверьте ping:**
   ```bash
   # С телефона пингуйте ноутбук
   ping 192.168.1.100
   ```

4. **Проверьте, что оба устройства в одной сети:**
   ```bash
   # IP адреса должны быть в одном диапазоне
   # Например: 192.168.1.x или 10.0.0.x
   ```

## Пример полного запуска

**Терминал 1 (Ноутбук):**
```bash
$ cargo run --bin core -- 8080
🚀 Starting MeshLink P2P Node
   Node ID: a1b2c3d4-e5f6-7890-abcd-ef1234567890
   Protocol Version: 1
Listening on: 0.0.0.0:8080
Incoming connection from 192.168.1.101:54321
Handshake successful with 192.168.1.101 (ID: z9y8x7w6-v5u4-3210-tsrq-ponmlkjihgf, protocol: 1)
Connected: z9y8x7w6-v5u4-3210-tsrq-ponmlkjihgf
Decrypted message from z9y8x7w6-v5u4-3210-tsrq-ponmlkjihgf
Heartbeat - Connected: 1/1 peers
```

**Терминал 2 (Телефон или второй терминал на ноутбуке):**
```bash
$ cargo run --bin core -- 8081 192.168.1.100:8080
🚀 Starting MeshLink P2P Node
   Node ID: z9y8x7w6-v5u4-3210-tsrq-ponmlkjihgf
   Protocol Version: 1
Listening on: 0.0.0.0:8081
TCP connection established to 192.168.1.100:8080
Connected to peer 192.168.1.100:8080 (ID: a1b2c3d4-e5f6-7890-abcd-ef1234567890, protocol: 1)
Connected: a1b2c3d4-e5f6-7890-abcd-ef1234567890
Heartbeat - Connected: 1/1 peers
```

## Что происходит под капотом

1. **Handshake:** Ноды обмениваются публичными RSA ключами
2. **Key Exchange:** Генерируется AES-256 сессионный ключ, шифруется RSA и отправляется
3. **Encryption:** Все последующие сообщения шифруются AES-256-GCM
4. **Heartbeat:** Каждые 5 секунд отправляются ping/pong для keepalive
5. **Discovery:** UDP broadcast для автоматического обнаружения пиров

## Следующие шаги

После успешного подключения можно:
- Отправлять mesh-сообщения между нодами
- Добавить больше нод в сеть
- Тестировать маршрутизацию через несколько хопов
- Интегрировать AI routing для оптимизации путей

