# 🚀 Быстрый старт для демонстрации

## Вариант 1: Автоматический скрипт (рекомендуется)

```bash
# Запустить все узлы автоматически
./scripts/start_demo_nodes.sh
```

Скрипт:
- ✅ Остановит все существующие узлы
- ✅ Удалит мешающий файл `~/.elysium_api_port`
- ✅ Запустит 3 узла
- ✅ Определит реальные API порты из логов
- ✅ Проверит статус каждого узла
- ✅ Покажет команды для использования

---

## Вариант 2: Ручной запуск

### Шаг 1: Остановить все узлы

```bash
pkill -f "ely start"
rm -f ~/.elysium_api_port
```

### Шаг 2: Запустить узлы

**Terminal 1:**
```bash
ely start 8080 --gateway 8000 -d
```

**Terminal 2:**
```bash
ely start 8081 127.0.0.1:8080 --gateway 8001 -d
```

**Terminal 3:**
```bash
ely start 8082 127.0.0.1:8081 --gateway 8002 -d
```

**Подождать 2-3 секунды для подключения**

### Шаг 3: Узнать реальные API порты

```bash
# Проверить логи каждого узла
grep "API server listening" .ely/node-8080/node-8080.log
grep "API server listening" .ely/node-8081/node-8081.log
grep "API server listening" .ely/node-8082/node-8082.log
```

Или проще:
```bash
# Показать все открытые порты ely
lsof -i -P | grep LISTEN | grep ely | grep -E "170[0-9][0-9]"
```

### Шаг 4: Проверить статус

```bash
# Использовать порты из шага 3
ely status --port 17080  # Узел 1 (замените на реальный порт!)
ely status --port 17082  # Узел 2 (замените на реальный порт!)
ely status --port 17084  # Узел 3 (замените на реальный порт!)
```

**Каждый должен показать уникальный Node ID!**

---

## 📋 Примеры команд

### Публикация контента

```bash
# На первом узле
ely publish site/index.html "<h1>Hello</h1>" --port 17080

# Скопировать Node ID из вывода
# Например: ely://FzmzADh51zHLPNpea9pTVY3ynyjZTuN1VNWji5sf7Jk1/site/index.html
```

### Получение контента

```bash
# На третьем узле (через mesh)
ely fetch ely://<node_id_1>/site/index.html --port 17084
```

### Показать пиры

```bash
ely peers --port 17080  # Пиры первого узла
ely peers --port 17082  # Пиры второго узла
ely peers --port 17084  # Пиры третьего узла
```

### Открыть в браузере

```bash
# После публикации, открыть в браузере
# http://localhost:8000/ely/<node_id>/site/index.html
```

---

## ⚠️ Важно

1. **Всегда используйте `--port`** - без него все команды подключатся к одному узлу
2. **Проверяйте реальные API порты** - они могут отличаться от `9000 + P2P порт`
3. **Каждый узел должен иметь уникальный Node ID** - если одинаковые, значит подключаетесь к одному узлу

---

## 🐛 Troubleshooting

### Все показывают один Node ID

**Причина:** Используете один и тот же API порт для всех команд.

**Решение:**
```bash
# Узнать реальные API порты
lsof -i -P | grep LISTEN | grep ely

# Использовать разные порты
ely status --port 17080  # Для узла 1
ely status --port 17082  # Для узла 2
ely status --port 17084  # Для узла 3
```

### Не могу подключиться к порту

**Причина:** Узел не запущен или порт неправильный.

**Решение:**
```bash
# Проверить, запущен ли узел
ps aux | grep "ely start" | grep -v grep

# Проверить логи
tail -20 .ely/node-8080/node-8080.log
```

---

## ✅ Готово!

Теперь у вас запущены 3 узла, и вы можете:
- ✅ Публиковать контент на любом узле
- ✅ Получать контент через mesh сеть
- ✅ Открывать в браузере через Web Gateway
- ✅ Использовать Bundle Protocol

**Удачи с демонстрацией! 🎬**
