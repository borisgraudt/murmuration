# MeshLink - Пошаговая инструкция

## Шаг 1: Терминал 1 - Запуск первой ноды

Откройте первый терминал и выполните:

```bash
cd /Users/boris/Desktop/meshlink
cargo run --bin core -- 8082
```

**Что вы увидите:**
```
INFO: Created new node with ID: <uuid>
INFO: 🚀 Starting MeshLink P2P Node
INFO: Listening on: 0.0.0.0:8082
INFO: API server listening on 127.0.0.1:17082
INFO: Listening for incoming connections on 0.0.0.0:8082
```

**Важно:** Дождитесь сообщения `API server listening on 127.0.0.1:17082` - это значит нода готова.

**Оставьте этот терминал открытым!**

---

## Шаг 2: Терминал 2 - Запуск второй ноды

Откройте второй терминал и выполните:

```bash
cd /Users/boris/Desktop/meshlink
cargo run --bin core -- 8083 127.0.0.1:8082
```

**Что вы увидите:**
```
INFO: Created new node with ID: <uuid>
INFO: 🚀 Starting MeshLink P2P Node
INFO: Listening on: 0.0.0.0:8083
INFO: API server listening on 127.0.0.1:17083
INFO: Connected to peer 127.0.0.1:8082
```

**Важно:** Дождитесь сообщения `Connected to peer 127.0.0.1:8082` - это значит ноды подключились.

**Оставьте этот терминал открытым!**

---

## Шаг 3: Терминал 3 - Визуализация (опционально)

Откройте третий терминал и выполните:

```bash
cd /Users/boris/Desktop/meshlink
cargo run --bin viz
```

**Что вы увидите:**
- Граф сети с нодами и связями
- Статистику внизу экрана
- Анимации при событиях

**Управление:** Нажмите `q` для выхода

**Оставьте этот терминал открытым!**

---

## Шаг 4: Терминал 4 - CLI команды

Откройте четвертый терминал для CLI команд.

### 4.1. Проверить статус

```bash
cd /Users/boris/Desktop/meshlink
MESHLINK_API_PORT=17082 cargo run --bin cli -- status
```

**Вывод:**
```
Node Status:
------------------------------------------------------------
  Node ID: <uuid>
  Connected: 1
  Total peers: 1
```

### 4.2. Посмотреть список пиров

```bash
MESHLINK_API_PORT=17082 cargo run --bin cli -- peers
```

**Вывод:**
```
Peers (1):
------------------------------------------------------------
  <peer_id> @ 127.0.0.1:8083 [Connected]
```

**Важно:** Скопируйте `<peer_id>` из вывода - он понадобится для отправки сообщений.

### 4.3. Отправить сообщение всем (broadcast)

```bash
MESHLINK_API_PORT=17082 cargo run --bin cli -- broadcast "Hello MeshLink!"
```

**Вывод:**
```
✓ Message sent! ID: <message_id>
```

### 4.4. Отправить сообщение конкретному пиру

```bash
MESHLINK_API_PORT=17082 cargo run --bin cli -- send <peer_id> "Hello peer!"
```

Замените `<peer_id>` на реальный ID из команды `peers`.

**Вывод:**
```
✓ Message sent! ID: <message_id>
```

---

## Полный пример всех команд

### Терминал 1:
```bash
cd /Users/boris/Desktop/meshlink
cargo run --bin core -- 8082
```

### Терминал 2:
```bash
cd /Users/boris/Desktop/meshlink
cargo run --bin core -- 8083 127.0.0.1:8082
```

### Терминал 3 (визуализация):
```bash
cd /Users/boris/Desktop/meshlink
cargo run --bin viz
```

### Терминал 4 (CLI):
```bash
cd /Users/boris/Desktop/meshlink

# Статус
MESHLINK_API_PORT=17082 cargo run --bin cli -- status

# Пиры
MESHLINK_API_PORT=17082 cargo run --bin cli -- peers

# Broadcast
MESHLINK_API_PORT=17082 cargo run --bin cli -- broadcast "Test message"

# Отправить конкретному пиру (замените <peer_id>)
MESHLINK_API_PORT=17082 cargo run --bin cli -- send <peer_id> "Hello!"
```

---

## Troubleshooting

### CLI не подключается

**Проблема:** `Error: Connection refused`

**Решение:**
1. Убедитесь, что нода запущена в Терминале 1
2. Проверьте, что в логах есть `API server listening on 127.0.0.1:17082`
3. Используйте правильный API порт: `MESHLINK_API_PORT=17082`

### Ноды не подключаются

**Проблема:** `Handshake failed: early eof`

**Решение:**
1. Убедитесь, что первая нода запущена и слушает
2. Проверьте, что порты свободны: `lsof -i :8082`
3. Запускайте ноды последовательно (сначала 8082, потом 8083)

### Порт занят

**Проблема:** `Address already in use`

**Решение:**
1. Найдите процесс: `lsof -i :8082`
2. Остановите его или используйте другие порты (8084, 8085)

---

## Быстрая справка по API портам

- Нода на порту 8082 → API на порту 17082
- Нода на порту 8083 → API на порту 17083
- Формула: API порт = 10000 + node_port

Для CLI используйте: `MESHLINK_API_PORT=<api_port>`

